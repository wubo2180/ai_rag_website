# 知识抽取智能体表格显示问题诊断

## 问题描述
知识抽取智能体处理完文档后，结果没有以表格形式展现在页面上。

## 可能的原因

### 1. 后端返回数据格式不正确

#### 预期的数据格式
```json
{
  "status": "success",
  "data": {
    "extracted_knowledge": [
      {
        "字段1": "值1",
        "字段2": "值2",
        "字段3": "值3"
      },
      {
        "字段1": "值4",
        "字段2": "值5",
        "字段3": "值6"
      }
    ],
    "elapsed_time": 1.23
  }
}
```

#### 关键要求
- `extracted_knowledge` 必须是一个**数组**
- 数组中的每个元素必须是一个**对象**
- 所有对象应该有相同的**键**（表头）

### 2. 常见的错误格式

#### 错误1: 返回的是字符串
```json
{
  "extracted_knowledge": "这是文本格式的结果..."
}
```
❌ 这样会显示为文本，不会显示表格

#### 错误2: 返回的是单个对象
```json
{
  "extracted_knowledge": {
    "字段1": "值1",
    "字段2": "值2"
  }
}
```
❌ 这样会显示为文本，不会显示表格

#### 错误3: 数组为空
```json
{
  "extracted_knowledge": []
}
```
❌ 空数组不会显示表格

#### 错误4: 嵌套结构错误
```json
{
  "result": {
    "extracted_knowledge": [...]
  }
}
```
❌ 数据层级不对

## 已实现的修复

### 1. 添加调试信息面板
在结果区域顶部添加了一个可展开的调试面板，显示：
- 数据类型
- 是否为数组
- 数组长度
- 表头信息
- 完整的原始JSON数据

### 2. 增强的日志输出
在浏览器控制台会输出：
```javascript
console.log('API响应:', response.data)
console.log('抽取结果:', extractionResult.value)
console.log('知识数据:', extractionResult.value.extracted_knowledge)
console.log('数据类型:', typeof extractionResult.value.extracted_knowledge)
console.log('是否数组:', Array.isArray(extractionResult.value.extracted_knowledge))
```

### 3. 自动切换到表格视图
如果数据格式正确，会自动切换到表格视图：
```javascript
if (Array.isArray(extractionResult.value.extracted_knowledge) && 
    extractionResult.value.extracted_knowledge.length > 0) {
  viewMode.value = 'table'
}
```

### 4. 友好的错误提示
- 如果数据不是数组格式，会显示警告信息
- 自动将对象转换为JSON字符串显示

## 诊断步骤

### 步骤1: 查看调试信息
1. 运行知识抽取
2. 查看结果页面顶部的"调试信息"
3. 点击展开，检查：
   - `是否数组` 应该是 `true`
   - `数组长度` 应该 > 0
   - `表头数量` 应该 > 0

### 步骤2: 检查浏览器控制台
1. 按F12打开开发者工具
2. 查看Console标签页
3. 找到相关的日志输出
4. 确认数据格式

### 步骤3: 检查原始JSON
1. 在调试信息中查看"原始数据"
2. 确认`extracted_knowledge`字段的格式
3. 确认是否符合预期的数组格式

## 解决方案

### 方案1: 修复后端返回格式
如果后端返回格式不对，需要修改后端代码：

```python
# 错误示例
return {
    "status": "success",
    "data": {
        "extracted_knowledge": "文本结果..."  # ❌ 错误
    }
}

# 正确示例
return {
    "status": "success",
    "data": {
        "extracted_knowledge": [
            {"name": "张三", "age": 25},
            {"name": "李四", "age": 30}
        ],  # ✅ 正确
        "elapsed_time": 1.23
    }
}
```

### 方案2: 前端数据转换
如果后端格式无法修改，可以在前端转换：

```javascript
// 在processFile函数中添加
if (response.data.status === 'success') {
  let result = response.data.data
  
  // 如果extracted_knowledge不是数组，尝试转换
  if (!Array.isArray(result.extracted_knowledge)) {
    // 如果是对象，转换为单元素数组
    if (typeof result.extracted_knowledge === 'object') {
      result.extracted_knowledge = [result.extracted_knowledge]
    }
    // 如果是字符串，尝试JSON解析
    else if (typeof result.extracted_knowledge === 'string') {
      try {
        const parsed = JSON.parse(result.extracted_knowledge)
        if (Array.isArray(parsed)) {
          result.extracted_knowledge = parsed
        }
      } catch (e) {
        console.error('无法解析JSON:', e)
      }
    }
  }
  
  extractionResult.value = result
}
```

### 方案3: 检查API端点
确认使用的是正确的API端点：
- 当前使用: `/ai-service/knowledge-extraction/`
- 确认后端是否正确实现了这个接口

## 测试用例

### 测试数据1: 正常格式
```json
{
  "status": "success",
  "data": {
    "extracted_knowledge": [
      {"原材料": "聚醚多元醇", "分子量": "1000", "供应商": "XX公司"},
      {"原材料": "异氰酸酯", "分子量": "500", "供应商": "YY公司"}
    ],
    "elapsed_time": 1.5
  }
}
```
✅ 应该显示2行3列的表格

### 测试数据2: 空数组
```json
{
  "status": "success",
  "data": {
    "extracted_knowledge": [],
    "elapsed_time": 0.5
  }
}
```
❌ 不会显示表格，显示"暂无数据"

### 测试数据3: 字符串格式
```json
{
  "status": "success",
  "data": {
    "extracted_knowledge": "找到3个原材料: 聚醚多元醇, 异氰酸酯, 催化剂",
    "elapsed_time": 1.0
  }
}
```
❌ 显示为文本，不是表格

## 下一步操作

### 立即操作
1. ✅ 重新运行知识抽取
2. ✅ 查看调试信息面板
3. ✅ 检查浏览器控制台
4. ✅ 复制原始JSON数据

### 根据诊断结果
- **如果是数组但为空**: 检查后端逻辑，确认是否正确提取了数据
- **如果不是数组**: 修改后端返回格式或使用前端转换方案
- **如果数据格式正确但不显示**: 检查是否有JavaScript错误

## 联系支持

如果以上方法都无法解决问题，请提供：
1. 调试信息面板的截图
2. 浏览器控制台的日志
3. 上传的文件类型和内容示例

---

**更新时间**: 2025年11月15日  
**版本**: v1.0  
**状态**: ✅ 已添加完整的调试功能
