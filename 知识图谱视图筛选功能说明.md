# 知识图谱视图筛选功能说明

## 功能概述
为知识图谱添加了灵活的视图模式筛选功能，用户可以根据需要只查看特定类型的节点和关系。

## 功能特性

### 1. 视图模式选项
在控制面板中提供下拉菜单，包含以下选项：

- **完整图谱** (`full`): 显示所有节点和关系
- **仅原材料** (`raw_material`): 只显示原材料类型的节点
- **仅中间体** (`intermediate`): 只显示中间体类型的节点
- **仅配方** (`formula`): 只显示配方类型的节点
- **仅性能数据** (`performance`): 只显示性能数据类型的节点

### 2. 智能过滤逻辑

#### 节点过滤
- 根据选择的视图模式，只显示对应类型的节点
- 例如选择"仅原材料"，只显示 `type === 'raw_material'` 的节点

#### 边过滤
- 自动过滤边（连线），只显示被选中节点之间的连接
- 避免显示悬空的边，保持图谱的逻辑完整性

```javascript
// 过滤节点
filteredNodesData = graphData.nodes.filter(node => node.type === viewMode.value)

// 过滤边 - 只保留两端都是选中节点的边
const nodeIds = new Set(filteredNodesData.map(n => n.id))
filteredEdgesData = graphData.edges.filter(edge => 
  nodeIds.has(edge.source) && nodeIds.has(edge.target)
)
```

### 3. 用户交互

#### 切换方式
1. 点击下拉框
2. 选择想要的视图模式
3. 图谱自动重新渲染

#### 反馈提示
- 切换时显示提示消息：`切换到: [视图名称]`
- 控制台输出详细的过滤信息，便于调试

### 4. 技术实现

#### 响应式数据
```javascript
const viewMode = ref('full')  // 默认完整图谱
```

#### 事件处理
```javascript
const handleViewModeChange = (value) => {
  console.log('切换视图模式:', value)
  ElMessage.info(`切换到: ${modeLabels[value]}`)
  renderGraph()  // 重新渲染
}
```

#### 渲染优化
- 在 `renderGraph()` 函数中根据 `viewMode` 过滤数据
- 使用过滤后的数据创建节点和边
- 保持原始数据不变，便于切换回完整视图

## 使用场景

### 场景1: 分析原材料供应链
- 选择"仅原材料"
- 查看所有原材料之间的关系
- 分析供应商分布

### 场景2: 研究中间产物
- 选择"仅中间体"
- 专注于中间体的转化过程
- 优化中间体配方

### 场景3: 配方对比分析
- 选择"仅配方"
- 比较不同配方的组成
- 找出最优配方方案

### 场景4: 性能评估
- 选择"仅性能数据"
- 查看性能测试结果
- 分析性能趋势

## 优势

### 1. 简化视图
- 减少视觉干扰
- 聚焦于特定类型的节点
- 提高分析效率

### 2. 灵活切换
- 无需刷新页面
- 实时响应
- 保持交互状态

### 3. 智能关联
- 自动过滤相关边
- 保持图谱逻辑完整
- 避免信息断层

### 4. 易于使用
- 直观的下拉菜单
- 清晰的选项说明
- 友好的提示信息

## 性能考虑

### 过滤性能
- 使用 `filter()` 方法，时间复杂度 O(n)
- Set 查找，时间复杂度 O(1)
- 对于大规模数据集依然高效

### 渲染优化
- 只渲染必要的节点和边
- 减少 ECharts 计算负担
- 提升响应速度

## 扩展可能

### 未来可以添加：
1. **多选模式**: 同时显示多种类型的节点
2. **保存视图**: 记住用户的视图偏好
3. **快捷键**: 使用键盘快速切换视图
4. **视图预设**: 保存和加载自定义视图配置
5. **导出功能**: 导出当前视图的图片或数据

## 代码位置

- **模板**: `KnowledgeGraph.vue` 第51行 - 下拉选择框
- **逻辑**: `KnowledgeGraph.vue` 第461-470行 - 过滤逻辑
- **处理**: `KnowledgeGraph.vue` 第832-850行 - 事件处理函数

---

**开发日期**: 2025年11月11日  
**版本**: v1.1  
**状态**: ✅ 已完成并测试
